# Optional: point the app at your Kinly tRPC server.
# Leave unset to run the app fully offline against on-device sample data.
# On a real device use your computer's LAN IP, not localhost.
# EXPO_PUBLIC_SERVER_URL=http://localhost:3000
